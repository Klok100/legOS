#ifndef _SYSCALL_LINKAGE_H
#define _SYSCALL_LINKAGE_H

/* Define wrapper as a callable function, so that we can utilise */
/* the wrapper properly.                                         */
extern void syscall_wrapper( void );

#endif
